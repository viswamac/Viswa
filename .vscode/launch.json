{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome for Angular",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}/Angular"
    }
  ]
}
